// Using AJAX, this refreshes the user input text area
// on the home page with the summarized verison
$('#home-user-input-form').html("<%= escape_javascript(render 'user_input', locals: {user_input: @input}) %>");


// This activates clipboard.js
(function(){
    new Clipboard('#copy-button');
})();
